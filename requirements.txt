# Backward-compatible convenience requirements for the pyOASIS wrapper.
#
# Prefer the workflow-specific files:
#   requirements-pyoasis.txt
#   requirements-pytecgg.txt
#
# The full analysis tree uses additional dependencies documented in the
# corresponding module READMEs. Exact tested versions should be frozen from the
# validated environment for strict archival reproducibility.
-r requirements-pyoasis.txt
